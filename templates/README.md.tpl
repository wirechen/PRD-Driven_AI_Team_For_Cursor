# {{PROJECT_NAME}}

## 一、项目概述

{{PROJECT_DESC}}

### 技术架构

| 模块 | 技术栈 |
|------|--------|
{{TECH_TABLE}}

---

## 二、团队架构与角色职责

### 2.1 角色分工

```
{{TEAM_DIAGRAM}}
```

### 2.2 主 Agent（项目经理）

**身份**：项目总调度，专注任务管理和团队协调，不直接做产品设计或编码。

**职责**：
- 接收用户需求，派发给 product-agent 进行产品设计
- 将产品方案拆解为开发任务，分派给开发子代理
- 统一协调子代理的工作，充当通信中枢
- 审核子代理的交付物（PRD、代码、测试报告）
- 分析测试报告，将 Bug 分流给对应的子代理
- 每阶段完成后组织验收，确认质量后交付用户
- **并行调度**：在同一条消息中同时启动多个子代理，最大化效率

**重要规则**：
- 子代理之间**不能直接通信**，所有协作必须经过主 Agent 中转
- 主 Agent 负责决定何时推进到下一开发阶段
- 产品设计工作**必须委托给 product-agent**，主 Agent 不直接撰写 PRD

{{AGENT_ROLES_DETAIL}}

---

## 三、开发流程规范

### 3.1 PRD 驱动开发（核心规范）

**PRD（`docs/PRD.md`）是本项目的唯一需求源和开发基准。** 所有功能开发、修改、优化都必须以 PRD 为依据。

#### 3.1.1 PRD 的地位

- PRD 是用户与团队之间的唯一契约，用户通过审阅 PRD 了解系统的全部行为
- 用户不需要也不会去阅读代码，因此 **PRD 必须完整、准确、可理解**
- 任何与 PRD 描述不一致的实现都视为 Bug

#### 3.1.2 PRD 内容标准

每个功能模块的 PRD 描述必须包含以下维度：

| 维度 | 说明 | 示例 |
|------|------|------|
| **页面布局** | 页面各区域的结构、组件类型 | "顶部统计栏展示3个卡片" |
| **数据展示** | 每个列表/表格的列定义、字段格式 | "表格列：姓名/手机号/状态/操作" |
| **用户交互** | 按钮行为、弹窗内容、表单规则 | "点击'删除'弹出确认弹窗" |
| **状态流转** | 业务状态变化的条件和结果 | "draft→published→active→ended" |
| **异常处理** | 边界场景的处理方式和用户提示 | "手机号重复时提示'该手机号已存在'" |
| **接口依赖** | 依赖的后端 API 列表及关键参数 | "`GET /api/v1/users?status=active`" |
| **验收标准** | 可测试的验收条件清单 | "- [ ] 列表正确分页" |

#### 3.1.3 PRD 更新时机（强制）

以下任何情况发生时，**必须先更新 PRD，再进行开发**：

- 新增功能模块
- 修改现有功能的行为逻辑
- 新增或调整 API 接口
- 修改页面布局或交互方式
- 修改业务规则
- 修复 Bug 时发现 PRD 描述与预期不符

#### 3.1.4 PRD 驱动的工作流

```
用户提出需求
  │
  ├── 1. 项目经理使用 Plan 模式与用户梳理需求细节
  │   └── 明确功能范围、交互逻辑、边界场景
  │
  ├── 2. 需求确认后，项目经理派发 product-agent 撰写/更新 PRD
  │   └── 用户审阅 PRD，确认需求描述无误
  │
  ├── 3. PRD 确认后，派发 frontend-agent 出 UI 设计图
  │   └── 用户审阅 UI 设计图，确认视觉和交互方案
  │
  ├── 4. 项目经理根据 PRD 拆解任务，并行分派开发
  │   ├── frontend-agent ← 严格按 PRD 页面描述 + UI 设计图实现
  │   └── backend-agent  ← 严格按 PRD 接口定义实现
  │
  ├── 5. test-agent 按 PRD 验收标准编写测试用例
  │
  ├── 6. Bug 修复后检查：修复是否与 PRD 一致？
  │   └── 若 PRD 本身有误 → 先修 PRD 再改代码
  │
  └── 7. 用户验收：对照 PRD 验证功能
```

**关键原则：代码服从 PRD，PRD 服从用户。**

### 3.2 阶段化开发

项目按功能模块分阶段推进：

```
阶段开始
  │
  ├── 1. 用户在 Plan 模式下与项目经理梳理需求
  │   └── 明确功能范围、交互逻辑、边界场景
  │
  ├── 2. product-agent 撰写/更新 PRD（包含页面布局、交互细节、验收标准）
  │   └── 用户审阅 PRD，确认后方可继续
  │
  ├── 3. frontend-agent 根据 PRD 出 UI 设计图
  │   └── 用户审阅 UI 设计图，确认后方可进入开发
  │
  ├── 4. 项目经理拆解任务并并行分派
  │   ├── frontend-agent ← 前端任务（参照 PRD + UI 设计图）
  │   └── backend-agent  ← 后端任务（参照 PRD 接口定义）
  │
  ├── 5. 开发阶段（并行）
  │
  ├── 6. 测试阶段
  │   └── test-agent 输出测试报告 → docs/test-report-phaseX.md
  │
  ├── 7. Bug 修复循环（并行分派）
  │   └── test-agent 回归测试 → 直到全部通过
  │
  └── 8. 用户对照 PRD 验收
```

### 3.3 通信规则

- 子代理之间 **不能直接通信**，也**不能嵌套调用**其他子代理
- 所有信息传递、任务分派、Bug 分流都通过主 Agent（项目经理）中转
- 主 Agent 通过在**同一条消息中并行启动多个 Task**实现真正并行

### 3.4 Bug 分流规则

| Bug 类型 | 分派对象 | 示例 |
|----------|----------|------|
| API 返回错误码/500 | backend-agent | 接口逻辑错误 |
| API 字段缺失/格式错误 | backend-agent | 返回字段名不对 |
| 页面显示异常 | frontend-agent | 数据不展示、样式错位 |
| 前端请求方法/参数错误 | frontend-agent | POST vs PUT |
| 前后端字段名不一致 | 双方各自修复 | 或统一改一端对齐 |

---

## 四、重要约定

1. **PRD 驱动开发**：`docs/PRD.md` 是唯一需求源。任何需求变更必须先更新 PRD，再进行开发
2. **PRD 内容完整性**：PRD 中每个功能必须包含 7 个维度，确保用户无需阅读代码即可理解系统行为
3. **代码服从 PRD**：实现与 PRD 不一致视为 Bug。若 PRD 本身有误，先修正 PRD 再改代码
4. **后端接口定义优先**：前端 API 调用的方法和参数名必须与后端一致
5. **每阶段必须测试闭环**：开发 → 测试 → Bug修复 → 回归 → 验收
6. **主 Agent 是项目经理**：专注调度和沟通，产品设计委托 product-agent，不直接编码
7. **子代理不直接通信**：所有协作通过主 Agent 中转

---

## 五、快速开始

> 本项目由 [PRD-Driven AI Team for Cursor](https://github.com/wirechen/PRD-Driven_AI_Team_For_Cursor) 初始化。

### 首次使用

1. 用 Cursor 打开本项目目录
2. 先使用 **Plan 模式**与主 Agent 聊需求细节：
   > "我想做一个XXX，帮我梳理下需求细节"
3. 需求明确后，让产品经理生成 PRD：
   > "请让产品经理根据刚才讨论的需求生成初版 PRD"
4. 审阅 PRD，确认无误后，让前端出 UI 设计图：
   > "PRD 没问题，让前端根据 PRD 出一版 UI 设计图"
5. 审阅 UI 设计图，确认后开始开发：
   > "UI 没问题，安排各工程师开始开发 Phase 1"

### 日常使用

- **提新需求**：告诉主 Agent，他会先用 Plan 模式帮你梳理，再派发产品经理更新 PRD
- **改需求**：直接修改 `docs/PRD.md`，然后告诉主 Agent 哪里改了
- **验收**：对照 PRD 验证功能，发现问题告诉主 Agent

**你只需要关心 PRD，其余交给 AI 团队。**
